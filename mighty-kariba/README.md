# Mighty Kariba

CSS Zen Garden submission by **Roy Gitonga**.

## Files

| File | Goes to |
| --- | --- |
| `mighty-kariba.css` | `mighty-kariba/mighty-kariba.css` in the repo |
| `mighty-kariba-web.jpg` | `mighty-kariba/mighty-kariba-web.jpg` (same folder as the CSS) |
| `masterlist-entry.php` | append the array to `$designList` in `includes/masterlist.php` |
| `zengarden-item.xml` | insert the `<item>` at the top of `<channel>` in `zengarden.xml` |

## Palette

Green `#198A00` · Red `#DE2010` · Black `#000000` · Orange `#EF7D00`
(`#FFFFFF` is body text only.)

## Notes

- Photograph "Mighty Kariba" (Kariba Dam) by Roy Gitonga, own work. Web copy is 2000px / ~620 KB.
- Type is Bricolage Grotesque + Karla, loaded from Google Fonts via `@import` — no Typekit kit ID required.
- No changes to the shared `index.html`.
- Test locally at `http://localhost/?cssfile=mighty-kariba/mighty-kariba.css`.
